apiVersion: v1
kind: Service
metadata:
  name: yiiiiy
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yiiiiy"
    port: 8080
  selector:
    app: yiiiiy
